import 'package:flutter/services.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_screenshot_listener/flutter_screenshot_listener.dart';

void main() {
  /*const MethodChannel channel = MethodChannel('flutter_screenshot_listener');

  setUp(() {
    channel.setMockMethodCallHandler((MethodCall methodCall) async {
      return '42';
    });
  });

  tearDown(() {
    channel.setMockMethodCallHandler(null);
  });

  test('getPlatformVersion', () async {
    expect(await FlutterScreenshotListener.platformVersion, '42');
  });*/
}
